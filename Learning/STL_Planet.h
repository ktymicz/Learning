#pragma once
//#include <array>
#include <vector>
#include <algorithm>
#include <iostream>
#include <span>
#include <cstddef> // std::size_t


// Land_of_Permutation

	// Province_of_Heaps
	// make_heap
void make_heap_stl();

	// pop_heap
	// push_heap
	// sort_heap

// Secret_Runes

// Land_of_Queries
namespace LOQ{
	struct MyStruct
	{
		std::string name;
		int value;
		
		 //bool operator()(int n) const { return value; };
		//operator int() { return value; };
		//friend auto operator==(MyStruct a, MyStruct b) -> bool;

	};
	//auto operator==(MyStruct a, MyStruct b) -> bool;
	//auto operator
	void querying_property_stl();
}






// Glorious_County_of_Algos_on_Sets
